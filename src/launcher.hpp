#pragma once
#include <unistd.h>

// ─────────────────────────────────────────────────────────────────────────────
//  Запуск Dota 2 через Steam и ожидание готовности процесса.
// ─────────────────────────────────────────────────────────────────────────────
namespace launcher {

// Запустить Dota 2 (steam://run/570) от имени вызвавшего sudo пользователя и ждать
// появления процесса dota2. Возвращает PID или 0 при таймауте.
pid_t launchAndWaitForDota();

// Дождаться, пока libclient.so отмапится в адресное пространство процесса.
// Нужно для код-патчей: патчить можно только после загрузки .so.
bool waitForClientLib(pid_t pid, int timeoutSec = 60);

} // namespace launcher
